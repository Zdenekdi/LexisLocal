<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Entitlements pro HARDENED RUNTIME (Developer ID, mimo Mac App Store).
  LexisLocal je Electron, který navíc FORKUJE Node backend (backend/server.js)
  a načítá nativní modul (canvas). Hardened runtime by jinak takový kód zabil,
  proto tyto výjimky:
    • allow-jit / allow-unsigned-executable-memory — JIT ve V8 (Electron),
    • allow-dyld-environment-variables — Electron/fork předává proměnné prostředí,
    • disable-library-validation — načtení nativních modulů a forknutého Node,
    • network.client — aplikace je síťový klient (lokální Ollama, registry).
  ŽÁDNÝ App Sandbox (Developer ID ho nevyžaduje) — spisy na ploše zůstanou přístupné.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
</dict>
</plist>
