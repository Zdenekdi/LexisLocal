name: Build/Release LexisLocal

on:
  push:
    branches:
      - main
    paths:
      - 'package.json'
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    strategy:
      matrix:
        os: [macos-latest, windows-latest]
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Build and Publish Electron App
        run: |
          git log -1 --pretty=%B > release-notes.txt
          if [ "$RUNNER_OS" == "Windows" ]; then
            npx electron-builder --win --x64 --publish always -c.releaseInfo.releaseNotesFile=release-notes.txt
          else
            npx electron-builder --mac zip --arm64 --publish always -c.releaseInfo.releaseNotesFile=release-notes.txt
          fi
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
