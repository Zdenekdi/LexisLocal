version: "3.8"

services:
  broker:
    image: docker.io/library/redis:7-alpine
    container_name: lexis-paperless-redis
    restart: unless-stopped
    volumes:
      - redisdata:/data

  db:
    image: docker.io/library/postgres:16-alpine
    container_name: lexis-paperless-db
    restart: unless-stopped
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: paperless
      POSTGRES_USER: paperless
      # Secrety se NIKDY necommitují — načítají se z .env (viz .env.example).
      POSTGRES_PASSWORD: ${PAPERLESS_DB_PASSWORD:?Nastavte PAPERLESS_DB_PASSWORD v souboru .env}

  webserver:
    image: ghcr.io/paperless-ngx/paperless-ngx:latest
    container_name: lexis-paperless-webserver
    restart: unless-stopped
    depends_on:
      - broker
      - db
    ports:
      - "8000:8000"
    volumes:
      - ./docker/paperless/data:/usr/src/paperless/data
      - ./docker/paperless/media:/usr/src/paperless/media
      - ./docker/paperless/consume:/usr/src/paperless/consume
      - ./docker/paperless/export:/usr/src/paperless/export
    environment:
      PAPERLESS_REDIS: redis://broker:6379
      PAPERLESS_DBHOST: db
      PAPERLESS_DBPORT: 5432
      PAPERLESS_DBNAME: paperless
      PAPERLESS_DBUSER: paperless
      PAPERLESS_DBPASS: ${PAPERLESS_DB_PASSWORD:?Nastavte PAPERLESS_DB_PASSWORD v souboru .env}
      PAPERLESS_SECRET_KEY: ${PAPERLESS_SECRET_KEY:?Nastavte PAPERLESS_SECRET_KEY v souboru .env}
      PAPERLESS_TIME_ZONE: Europe/Prague
      PAPERLESS_OCR_LANGUAGE: ces+eng
      PAPERLESS_OCR_LANGUAGES: ces eng
      # Auto-matching, tagging and machine learning settings
      PAPERLESS_FILENAME_FORMAT: "{created_year}/{correspondent}/{title}"
      # Allow local webhooks to access host (for communicate back to LexisLocal API)
      PAPERLESS_WEBHOOKS_ALLOWED_SCHEMES: http,https
      PAPERLESS_WEBHOOKS_ALLOWED_PORTS: 4000,3000,80,443

  lexis-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lexis-backend
    restart: unless-stopped
    ports:
      - "4000:4000"
    volumes:
      - ~/Desktop/LexisSpisy:/app/LexisSpisy
    environment:
      - WATCH_DIR=/app/LexisSpisy
      - PORT=4000
    extra_hosts:
      - "host.docker.internal:host-gateway"

volumes:
  redisdata:
  pgdata:
